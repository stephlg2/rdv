# Durcissement serveur (réf.)

- `rdv-intrusion-watch.sh` → `/usr/local/sbin/`
- `rdv-intrusion-watch.cron` → `/etc/cron.d/rdv-intrusion-watch`
- Alerte mail : stephlg2@orange.fr (toutes les 15 min si anomalie)
