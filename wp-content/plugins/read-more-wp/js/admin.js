/**
 * Read More Without Refresh Pro - Admin (v5.0)
 *
 * @package ReadMoreWithoutRefreshPro
 */

(function ($) {
    'use strict';

    $(function () {
        if ($.fn.wpColorPicker) {
            $('.cpa-color-picker').wpColorPicker();
        }
    });
})(jQuery);
